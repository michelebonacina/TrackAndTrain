import { WaypointType.Model } from './waypoint-type.model';

describe('WaypointType.Model', () => {
  it('should create an instance', () => {
    expect(new WaypointType.Model()).toBeTruthy();
  });
});
